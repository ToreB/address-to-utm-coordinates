# frozen_string_literal: true
source "https://rubygems.org"

# gets from github, due to pull request with bugfixes not having been merged into main repo
gem 'geoutm', :git => 'https://github.com/zydeco/geoutm'
